<h1>Editing account</h1>

<%= link_to 'Show', @account %> |
<%= link_to_function 'Back', 'history.back()' %>

<%= form_for(@account, :url=>account_path) do |f| %>
  <%= render :partial => "form", :locals=>{:f=>f} %>
<% end %>

<%= link_to 'top', account_path %>
